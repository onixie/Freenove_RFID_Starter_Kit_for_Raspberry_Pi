# Sample code in Rust

```sh
cargo run -p hello
```

```sh
cargo run -p blink
```

```sh
cargo run -p button_led
```

```sh
cargo run -p table_lamp
# or
cargo run -p table_lamp --features reactive
```